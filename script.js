(function () {
  'use strict';

  /* ─────────────────────────────────────────
     TRADUCCIONES COMPLETAS EN / ES
  ───────────────────────────────────────── */
  const i18n = {
    en: {
      // Navbar
      nav_cta:           "Let's talk",
      // Hero
      badge:             'Applied Intelligence',
      h1_line1:          'Technology',
      h1_line2:          'with vision.',
      hero_sub:          'We design and develop apps, systems, platforms and digital ecosystems built to solve, scale and create real value.',
      hero_cta_primary:  'Start a project',
      hero_cta_ghost:    'Who we are',
      stat1:             'Apps & Platforms',
      stat2:             'Intelligent Systems',
      stat3:             'Xoria Integrated',
      stat4:             'Real Market Execution',
      stat5:             'Strategic Product Design',
      // About
      about_eyebrow:     'Who we are',
      about_title:       'ALQIA',
      about_lead:        'ALQIA is a technology studio focused on building apps, systems, platforms and digital products with strategic vision.',
      about_body:        "We don't rely on empty formulas or market copies. We build from a real understanding of the problem, the operating context and the growth potential of each project. We integrate strategy, functional architecture, design, development and commercial vision to turn complex ideas into clear, useful and scalable digital solutions.",
      p1_title:          'Strategy',
      p1_body:           'Every project starts from a clear understanding of the problem and the business opportunity.',
      p2_title:          'Architecture',
      p2_body:           'We design robust functional structures before writing a single line of code.',
      p3_title:          'Execution',
      p3_body:           'We deliver working products to market — not just concepts or presentations.',
      services_title:    'What we build',
      svc1:              'Mobile Apps',
      svc2:              'Web Platforms',
      svc3:              'Enterprise Systems',
      svc4:              'Custom SaaS Tools',
      svc5:              'Product Architecture',
      svc6:              'Strategic Digital Design',
      svc7:              'Implementation & Evolution',
      // Xoria
      xoria_eyebrow:     'Intelligence layer',
      xoria_sub:         'The intelligence that lives inside our builds.',
      xoria_body:        'Xoria is our executive assistant and applied intelligence layer. It integrates within apps, systems and platforms to provide personalization, contextual memory, operational continuity and intelligent process orchestration. Thanks to Xoria, our developments incorporate an additional dimension of applied intelligence that connects workflows, retains context, elevates operational utility and delivers more personalized, dynamic and adaptive experiences.',
      xf1:               'Contextual memory',
      xf2:               'Process orchestration',
      xf3:               'Adaptive personalization',
      xf4:               'Workflow integration',
      // Work
      work_eyebrow:      'Execution',
      work_title:        'From vision to market.',
      work_intro:        'Our work does not stop at concept. At ALQIA we have delivered products to market, reflecting our capacity to convert strategy, design and development into ready solutions.',
      phil_title:        'Technology with intention.',
      phil_body:         'We believe in technology that serves, resolves, connects and evolves. We avoid development for its own sake or hollow trends. We build solutions with logic, purpose and real value.',
      // Contact
      contact_eyebrow:   'Ready to build',
      contact_title:     "Let's talk about what you're ready to build.",
      contact_sub:       'If you have a vision, an operational need or a market opportunity, ALQIA can turn it into a structured digital solution.',
      contact_cta:       'Start a conversation',
      contact_locations: 'CDMX & GDL, Mexico',
    },

    es: {
      // Navbar
      nav_cta:           'Hablemos',
      // Hero
      badge:             'Inteligencia Aplicada',
      h1_line1:          'Tecnología',
      h1_line2:          'con visión.',
      hero_sub:          'Diseñamos y desarrollamos apps, sistemas, plataformas y ecosistemas digitales pensados para resolver, escalar y generar valor real.',
      hero_cta_primary:  'Iniciar proyecto',
      hero_cta_ghost:    'Quiénes somos',
      stat1:             'Apps y Plataformas',
      stat2:             'Sistemas Inteligentes',
      stat3:             'Xoria Integrada',
      stat4:             'Ejecución Real de Mercado',
      stat5:             'Diseño Estratégico de Producto',
      // About
      about_eyebrow:     'Quiénes somos',
      about_title:       'ALQIA',
      about_lead:        'ALQIA es una firma de creación tecnológica enfocada en el desarrollo de apps, sistemas, plataformas y productos digitales con visión estratégica.',
      about_body:        'No partimos de fórmulas vacías ni de la copia del mercado. Construimos soluciones a partir de una comprensión real del problema, del entorno operativo y del potencial de crecimiento de cada proyecto. Integramos estrategia, arquitectura funcional, diseño, desarrollo y visión comercial para convertir ideas complejas en soluciones digitales claras, útiles y escalables.',
      p1_title:          'Estrategia',
      p1_body:           'Cada proyecto parte de una comprensión clara del problema y la oportunidad de negocio.',
      p2_title:          'Arquitectura',
      p2_body:           'Diseñamos estructuras funcionales robustas antes de escribir una sola línea de código.',
      p3_title:          'Ejecución',
      p3_body:           'Entregamos productos funcionando en el mercado, no solo conceptos o presentaciones.',
      services_title:    'Qué construimos',
      svc1:              'Apps Móviles',
      svc2:              'Plataformas Web',
      svc3:              'Sistemas Empresariales',
      svc4:              'Herramientas SaaS a la Medida',
      svc5:              'Arquitectura de Producto',
      svc6:              'Diseño Digital Estratégico',
      svc7:              'Implementación y Evolución',
      // Xoria
      xoria_eyebrow:     'Capa de inteligencia',
      xoria_sub:         'La inteligencia que vive dentro de nuestros desarrollos.',
      xoria_body:        'Xoria es nuestra asistente ejecutiva y capa de inteligencia aplicada. Se integra dentro de apps, sistemas y plataformas para aportar personalización, memoria contextual, continuidad operativa y orquestación inteligente de procesos. Gracias a Xoria, nuestros desarrollos pueden incorporar una dimensión adicional de inteligencia que conecta flujos, conserva contexto, eleva la utilidad operativa y ofrece experiencias más personalizadas, dinámicas y evolutivas.',
      xf1:               'Memoria contextual',
      xf2:               'Orquestación de procesos',
      xf3:               'Personalización adaptativa',
      xf4:               'Integración de flujos',
      // Work
      work_eyebrow:      'Ejecución',
      work_title:        'De la visión al mercado.',
      work_intro:        'Nuestro trabajo no se queda en concepto. En ALQIA hemos llevado desarrollos a ejecución real, incluyendo aplicaciones y soluciones publicadas que reflejan nuestra capacidad para convertir estrategia, diseño y desarrollo en productos listos para salir al mercado.',
      phil_title:        'Creamos tecnología con intención.',
      phil_body:         'Creemos en una tecnología que sirva, resuelva, conecte y evolucione. No nos interesa desarrollar por inercia ni seguir tendencias sin fondo. Nos interesa construir soluciones con lógica, propósito y valor real.',
      // Contact
      contact_eyebrow:   'Listos para construir',
      contact_title:     'Hablemos de lo que estás listo para construir.',
      contact_sub:       'Si tienes una visión, una necesidad operativa o una oportunidad de mercado, en ALQIA podemos convertirla en una solución digital con estructura real.',
      contact_cta:       'Iniciar conversación',
      contact_locations: 'CDMX y GDL, México',
    }
  };

  /* ─────────────────────────────────────────
     ESTADO
  ───────────────────────────────────────── */
  let currentLang = 'en';
  let currentView = 'home';

  /* ─────────────────────────────────────────
     APLICAR IDIOMA
  ───────────────────────────────────────── */
  function applyLang(lang) {
    const t = i18n[lang];
    document.querySelectorAll('[data-i18n]').forEach(el => {
      const key = el.getAttribute('data-i18n');
      if (t[key] === undefined) return;
      // Reemplaza ALQIA/Alqia con span Cocomat sin romper el resto
      const safe = t[key].replace(/(ALQIA|Alqia)/g, '<span class="brand-type">$1</span>');
      el.innerHTML = safe;
    });
    document.getElementById('langLabel').textContent = lang === 'en' ? 'ES' : 'EN';
    document.documentElement.lang = lang;
    currentLang = lang;
  }

  /* ─────────────────────────────────────────
     NAVEGACION SPA
  ───────────────────────────────────────── */
  function navigate(viewId) {
    if (viewId === currentView) return;

    const oldView = document.getElementById('view-' + currentView);
    const newView = document.getElementById('view-' + viewId);
    if (!newView) return;

    // Ocultar actual
    if (oldView) {
      oldView.style.opacity = '0';
      oldView.style.transform = 'translateY(12px)';
      oldView.style.transition = 'opacity .3s ease, transform .3s ease';
      setTimeout(() => { oldView.classList.remove('active'); }, 300);
    }

    // Mostrar nueva
    setTimeout(() => {
      newView.classList.add('active');
      newView.style.opacity = '0';
      newView.style.transform = 'translateY(18px)';
      requestAnimationFrame(() => {
        newView.style.transition = 'opacity .5s cubic-bezier(.22,.6,.36,1), transform .5s cubic-bezier(.22,.6,.36,1)';
        newView.style.opacity = '1';
        newView.style.transform = 'translateY(0)';
      });
      window.scrollTo({ top: 0, behavior: 'smooth' });
    }, 280);

    // Actualizar links activos (desktop + móvil)
    document.querySelectorAll('.nav-link, .mobile-nav-link').forEach(a => {
      a.classList.toggle('active', a.getAttribute('data-nav') === viewId);
    });

    currentView = viewId;
  }

  /* ─────────────────────────────────────────
     EVENTOS
  ───────────────────────────────────────── */
  // Toggle idioma
  document.getElementById('langToggle').addEventListener('click', () => {
    applyLang(currentLang === 'en' ? 'es' : 'en');
  });

  // Todos los elementos con data-nav (navbar, botones, brand)
  document.addEventListener('click', function (e) {
    const el = e.target.closest('[data-nav]');
    if (el) {
      e.preventDefault();
      navigate(el.getAttribute('data-nav'));
    }
  });

  /* ─────────────────────────────────────────
     NAVBAR scroll behavior
  ───────────────────────────────────────── */
  const navbar = document.getElementById('navbar');
  window.addEventListener('scroll', () => {
    navbar.style.boxShadow = window.scrollY > 10
      ? '0 1px 32px rgba(0,0,0,.5)'
      : 'none';
  }, { passive: true });

  /* ─────────────────────────────────────────
     NODOS TECH — canvas hero
  ───────────────────────────────────────── */
  /* ─────────────────────────────────────────
     NODOS TECH — canvas global (todas las páginas)
  ───────────────────────────────────────── */
  function initNodes() {
    const canvas = document.getElementById('nodesCanvas');
    if (!canvas) return;
    const ctx = canvas.getContext('2d');

    const MAX_DIST = 120;  // distancia máxima de conexión — más corta = menos telaraña
    const SPEED    = 0.3;
    let W, H, nodes;

    // Distribuye nodos en una cuadrícula con jitter para que queden uniformes
    function init() {
      W = canvas.width  = window.innerWidth;
      H = canvas.height = window.innerHeight;

      // Calcula cuántos nodos caben según el área (1 cada ~18000px²)
      const area    = W * H;
      const count   = Math.round(area / 18000);
      const NODES   = Math.max(18, Math.min(count, 55)); // entre 18 y 55

      const cols = Math.ceil(Math.sqrt(NODES * W / H));
      const rows = Math.ceil(NODES / cols);
      const cellW = W / cols;
      const cellH = H / rows;

      nodes = [];
      for (let c = 0; c < cols; c++) {
        for (let r = 0; r < rows; r++) {
          if (nodes.length >= NODES) break;
          const isOrange = Math.random() < 0.1;
          nodes.push({
            // posición base centrada en la celda + jitter de hasta 30%
            x:      (c + 0.5 + (Math.random() - .5) * 0.6) * cellW,
            y:      (r + 0.5 + (Math.random() - .5) * 0.6) * cellH,
            vx:     (Math.random() - .5) * SPEED,
            vy:     (Math.random() - .5) * SPEED,
            r:      Math.random() * 1.5 + 1,
            orange: isOrange
          });
        }
      }
    }

    function draw() {
      ctx.clearRect(0, 0, W, H);

      // Líneas
      for (let i = 0; i < nodes.length; i++) {
        for (let j = i + 1; j < nodes.length; j++) {
          const dx   = nodes[i].x - nodes[j].x;
          const dy   = nodes[i].y - nodes[j].y;
          const dist = Math.sqrt(dx * dx + dy * dy);
          if (dist < MAX_DIST) {
            const t     = 1 - dist / MAX_DIST;
            const isHot = nodes[i].orange || nodes[j].orange;
            ctx.beginPath();
            ctx.moveTo(nodes[i].x, nodes[i].y);
            ctx.lineTo(nodes[j].x, nodes[j].y);
            ctx.strokeStyle = isHot
              ? `rgba(249,128,88,${t * 0.30})`
              : `rgba(172,185,190,${t * 0.22})`;
            ctx.lineWidth = isHot ? 1.0 : 0.75;
            ctx.stroke();
          }
        }
      }

      // Puntos
      nodes.forEach(n => {
        if (n.orange) {
          ctx.shadowColor = 'rgba(249,128,88,0.7)';
          ctx.shadowBlur  = 8;
          ctx.beginPath();
          ctx.arc(n.x, n.y, n.r + 0.8, 0, Math.PI * 2);
          ctx.fillStyle = 'rgba(249,128,88,0.80)';
          ctx.fill();
          ctx.shadowBlur = 0;
        } else {
          ctx.beginPath();
          ctx.arc(n.x, n.y, n.r, 0, Math.PI * 2);
          ctx.fillStyle = 'rgba(172,185,190,0.55)';
          ctx.fill();
        }
      });
    }

    function update() {
      nodes.forEach(n => {
        n.x += n.vx;
        n.y += n.vy;
        if (n.x < 0 || n.x > W) n.vx *= -1;
        if (n.y < 0 || n.y > H) n.vy *= -1;
      });
    }

    function loop() {
      update();
      draw();
      requestAnimationFrame(loop);
    }

    window.addEventListener('resize', () => { init(); }, { passive: true });
    init();
    loop();
  }

  /* ─────────────────────────────────────────
     CURSOR — esfera naranja
  ───────────────────────────────────────── */
  function initCursor() {
    const el = document.getElementById('cursor');
    if (!el) return;

    let mx = window.innerWidth / 2;
    let my = window.innerHeight / 2;
    let cx = mx, cy = my;

    function lerp(a, b, t) { return a + (b - a) * t; }

    function loop() {
      cx = lerp(cx, mx, 0.12);
      cy = lerp(cy, my, 0.12);
      el.style.transform = `translate(${cx}px, ${cy}px) translate(-50%,-50%)`;
      requestAnimationFrame(loop);
    }

    window.addEventListener('mousemove', (e) => {
      mx = e.clientX;
      my = e.clientY;
    }, { passive: true });

    const hoverTargets = 'a, button, [data-nav], .nav-link, .stat-item, .svc-card, .pillar, .xpill, .phil-card, .mobile-nav-link';
    document.addEventListener('mouseover', (e) => {
      if (e.target.closest(hoverTargets)) document.body.classList.add('cursor-hover');
    });
    document.addEventListener('mouseout', (e) => {
      if (e.target.closest(hoverTargets)) document.body.classList.remove('cursor-hover');
    });

    document.addEventListener('mousedown', () => document.body.classList.add('cursor-click'));
    document.addEventListener('mouseup',   () => document.body.classList.remove('cursor-click'));

    loop();
  }

  /* ─────────────────────────────────────────
     TILT 3D interactivo en el icono del hero
  ───────────────────────────────────────── */
  function initIconTilt() {
    // Sin efecto de tilt — el icono es completamente estático
  }

  /* ─────────────────────────────────────────
     HAMBURGUESA — menú móvil
  ───────────────────────────────────────── */
  function initMobileMenu() {
    const burger   = document.getElementById('navBurger');
    const menu     = document.getElementById('mobileMenu');
    const mLinks   = document.querySelectorAll('.mobile-nav-link');
    const langMob  = document.getElementById('langToggleMob');
    const labelMob = document.getElementById('langLabelMob');
    if (!burger || !menu) return;

    function openMenu() {
      burger.classList.add('open');
      menu.classList.add('open');
      burger.setAttribute('aria-expanded', 'true');
      menu.setAttribute('aria-hidden', 'false');
      document.body.style.overflow = 'hidden';
    }
    function closeMenu() {
      burger.classList.remove('open');
      menu.classList.remove('open');
      burger.setAttribute('aria-expanded', 'false');
      menu.setAttribute('aria-hidden', 'true');
      document.body.style.overflow = '';
    }

    burger.addEventListener('click', () => {
      menu.classList.contains('open') ? closeMenu() : openMenu();
    });

    // Cerrar al seleccionar link
    mLinks.forEach(link => {
      link.addEventListener('click', () => {
        closeMenu();
        const target = link.getAttribute('data-nav');
        if (target) navigate(target);
      });
    });

    // Lang toggle duplicado en menú móvil
    if (langMob) {
      langMob.addEventListener('click', () => {
        document.getElementById('langToggle').click();
        // Sincronizar label
        setTimeout(() => {
          if (labelMob) labelMob.textContent = document.getElementById('langLabel').textContent;
        }, 10);
      });
    }

    // Cerrar con Escape
    document.addEventListener('keydown', e => {
      if (e.key === 'Escape') closeMenu();
    });
  }

  /* ─────────────────────────────────────────
     INICIO
  ───────────────────────────────────────── */
  applyLang('en');
  initNodes();
  initCursor();
  initIconTilt();
  initMobileMenu();

})();